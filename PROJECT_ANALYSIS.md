# مشروع Editorial - نظام إدارة المحتوى والمجلة الرقمية (Editorial CMS API)
**تاريخ التحليل:** 2026-08-01

---

## 1. 📌 نظرة عامة على المشروع (Project Overview)
مشروع **Editorial** هو **نظام إدارة محتوى إخباري ومجلة رقمية متكاملة (Editorial Content Management System - CMS API)** مبني على إطار العمل **Laravel 12 (PHP 8.3)**.

تم تصميم المشروع بهيكلية **RESTful API** مرنة وفصل كامل بين الخلفية (Backend) والواجهات الأمامية (Frontend)، مما يسمح بـ:
- تقديم المحتوى وتغذية منصات متعددة (مواقع الويب مثل منصة `Quick` المرفقة، تطبيقات الهواتف المحمولة، وأنظمة الشاشات).
- دعم التحكم الكامل بالمحتوى، المقالات، التصنيفات، الكُتّاب، التعليقات، الإعلانات، النشرة البريدية، وإعدادات الموقع.
- الاعتماد على **Laravel Sanctum** لإدارة جلسات المصادقة والـ API Tokens للمستخدمين والمحررين.

---

## 2. 🏗️ البنية التحتية والتقنيات المستخدمة (Tech Stack)

| التقنية / المكتبة | الغرض والاستخدام |
| :--- | :--- |
| **PHP 8.3+** | لغة البرمجة الأساسية للخلفية |
| **Laravel 12.x** | إطار العمل الرئيسي لبناء الـ REST API والموديلات والخدمات |
| **Laravel Sanctum** | إدارة المصادقة والأمان وإنشاء التوكينات (API Tokens) |
| **SQLite / MySQL** | قاعدة البيانات لتخزين البيانات والعلاقات |
| **Composer** | مدير حزم وإدارة اعتماديات PHP |
| **Postman Collection** | توثيق واختبار كافة نقاط النهاية (API Endpoints) |
| **Vite & JavaScript (Vanilla)** | تطوير واجهات العرض الأمامية التفاعلية |

---

## 3. 🗄️ هيكلية قاعدة البيانات والنماذج (Data Models & Architecture)

يحتوي النظام على **15 نموذجاً رئيسياً (Models)** تم تصميمها بعناية مع علاقات متكاملة وداعم لـ **Soft Deletes** و **UUIDs**:

### 1. **المقالات (`Article`)**
- **الوظيفة:** إدارة المقالات والأخبار المنشورة في الموقع.
- **الحقول الأساسية:** `uuid`, `title`, `slug`, `excerpt`, `content`, `reading_time`, `is_featured`, `allow_comments`, `published_at`, `status` (draft, published, archived), `views_count`, `meta_title`, `meta_description`.
- **العلاقات:**
  - ينتمي إلى قسم (`belongsTo Category`).
  - ينتمي إلى كاتب (`belongsTo Author`).
  - يرتبط بصورة بارزة (`belongsTo Media`).
  - يرتبط بوسوم متعددة (`belongsToMany Tag`).
  - يحتوي على تعليقات متعددة (`hasMany Comment`).
  - تتبع المنشئ والمحدّث (`created_by`, `updated_by`).

### 2. **الأقسام والتصنيفات (`Category`)**
- **الوظيفة:** تنظيم المحتوى والمقالات في أقسام فرعية ورئيسية.
- **الحقول الأساسية:** `name`, `slug`, `description`, `parent_id`, `image_id`, `order`, `is_active`.
- **العلاقات:** دعم الأقسام الهرمية (Parent/Child Categories) وعلاقة واحد إلى متعدد مع المقالات.

### 3. **الوسوم (`Tag`)**
- **الوظيفة:** تصنيف موضوعي للمقالات لسهولة البحث والاستكشاف.
- **الحقول الأساسية:** `name`, `slug`.

### 4. **المؤلفون والكُتّاب (`Author`)**
- **الوظيفة:** إدارة ملفات الكُتّاب والصحفيين المساهمين.
- **الحقول الأساسية:** `user_id`, `name`, `slug`, `bio`, `avatar_id`, `email`, `social_links` (JSON).

### 5. **التعليقات (`Comment`)**
- **الوظيفة:** إتاحة تفاعل القراء مع المقالات.
- **الحقول الأساسية:** `article_id`, `parent_id`, `user_id`, `guest_name`, `guest_email`, `content`, `status` (pending, approved, rejected).
- **الميزات:** دعم التعليقات الهرمية (الردود على التعليقات).

### 6. **الصفحات الثابتة (`Page`)**
- **الوظيفة:** إدارة صفحات الموقع الثابتة (مثل: من نحن، الشروط والأحكام، سياسة الخصوصية، والصفحة الرئيسية).
- **الحقول الأساسية:** `uuid`, `title`, `slug`, `content`, `template`, `is_homepage`, `status`, `published_at`.

### 7. **الإعلانات (`Advertisement`)**
- **الوظيفة:** نظام إدارة المساحات الإعلانية في الموقع.
- **الحقول الأساسية:** `uuid`, `title`, `location` (header, sidebar, in_article, footer), `type` (image, script, html), `image_id`, `destination_url`, `start_date`, `end_date`, `views_count`, `clicks_count`, `is_active`.

### 8. **رسائل التواصل (`ContactMessage`)**
- **الوظيفة:** استقبال استفسارات الزوار والرد عليها.
- **الحقول الأساسية:** `uuid`, `name`, `email`, `phone`, `subject`, `message`, `status` (new, read, replied, archived), `replied_at`, `handled_by`.
- **الميزات:** الرد المباشر على الزائر عبر الإيميل وتحديث الحالة تلقائياً إلى `replied`.

### 9. **مشتركو النشرة البريدية (`NewsletterSubscriber`)**
- **الوظيفة:** إدارة قائمة البريد الإخباري.
- **الحقول الأساسية:** `email`, `unsubscribe_token`, `status` (active, unsubscribed), `subscribed_at`.

### 10. **الوسائط والملفات (`Media`)**
- **الوظيفة:** مركز رفع وإدارة الصور والملفات.
- **الحقول الأساسية:** `file_name`, `file_path`, `mime_type`, `file_size`, `dimensions`.

### 11. **القوائم والتصفح (`Menu` & `MenuItem`)**
- **الوظيفة:** إدارة قوائم التصفح الهيدر والفوتر بشكل ديناميكي مع روابط للأقسام أو الصفحات أو المقالات أو روابط خارجية.

### 12. **إعدادات الموقع (`Setting`)**
- **الوظيفة:** تخزين واسترجاع إعدادات الموقع الديناميكية (اسم الموقع، الشعار، روابط التواصل، نصوص الفوتر، السكريبتات).

---

## 4. 🌐 نقاط النهاية الأساسية (API Endpoints Overview)

تم توفير مسارات متكاملة للـ REST API تحت البادئة `/api`:

### 📰 المقالات والأقسام (Articles & Categories)
- `GET /api/articles` - عرض قائمة المقالات مع Pagination والـ Eager Loading (Category, Tags, Author).
- `POST /api/articles` - إضافة مقال جديد وتوليد الـ Slug وتطبيق الوسوم.
- `GET /api/articles/{id}` - عرض تفاصيل مقال محدد.
- `PUT/PATCH /api/articles/{id}` - تحديث المقال والوسوم المرتبطة.
- `DELETE /api/articles/{id}` - حذف المقال (Soft Delete) وفك ارتباط الوسوم.
- `GET /api/categories` - عرض كافة الأقسام والتصنيفات.

### 📄 الصفحات والإعلانات (Pages & Advertisements)
- `GET /api/pages/homepage` - جلب بيانات الصفحة الرئيسية المنشورة.
- `GET /api/pages/slug/{slug}` - جلب الصفحة عن طريق الـ Slug.
- `GET /api/advertisements` - جلب الإعلانات المفعّلة وصورها.
- `POST /api/advertisements` - رفع صورة وإنشاء إعلان جديد.

### 📩 التواصل والنشرة البريدية (Contact & Newsletter)
- `POST /api/contact-messages` - إرسال رسالة تواصل جديدة من الزائر.
- `POST /api/contact-messages/{id}/reply` - إرسال رد بريدي مخصص للزائر وتحديث حالة الرسالة.
- `POST /api/newsletter-subscribers` - الاشتراك في النشرة البريدية.
- `POST /api/newsletter-subscribers/unsubscribe` - إلغاء الاشتراك باستخدام التوكين.

---

## 5. 🎯 المتطلبات الوظيفية للمشروع (Functional Requirements)

1. **إدارة المحتوى الصحفي والإخباري:**
   - كتابة ونشر المقالات وحفظ المسودات (Drafting & Publishing Flow).
   - جدول النشر الزمني للمقالات عبر حقل `published_at`.
   - حساب الوقت المتوقع لقراءة المقال تلقائياً أو يدوياً (`reading_time`).
   - تحديد المقالات المميزة (Featured Articles) للظهور في واجهة الصفحة الرئيسية.

2. **تحسين محركات البحث (SEO Requirements):**
   - توليد الـ Slugs الفريدة تلقائياً للأقسام والمقالات والصفحات لدعم اللغة العربية والإنجليزية.
   - دعم العناوين والوصف المخصص لمحركات البحث (`meta_title`, `meta_description`).

3. **التفاعل مع الجمهور (Engagement):**
   - تمكين التعليقات أو إغلاقها لكل مقال بشكل منفصل (`allow_comments`).
   - مراجعة التعليقات قبل نشرها (Pending Approval).
   - تتبع عدد المشاهدات وقراءات المقالات.

4. **إدارة الإعلانات والاستثمار (Monetization):**
   - توزيع الإعلانات حسب أماكن العرض (الهيدر، الشريط الجانبي، داخل المقال).
   - تتبع عدد الانطباعات (Views) والضغطات (Clicks) للقياس والتحليل.
   - تحديد فترة زمنية لتفعيل الإعلان وتوقيفه تلقائياً عند انتهاء المدة.

5. **خدمات التواصل والخدمات البريدية:**
   - نظام استقبال الرسائل ومتابعة حالتها (جديدة، مقروءة، تم الرد عليها).
   - إمكانية الرد المباشر من لوحة النظام لترسل إيميل رسمي للعميل/الزائر.
   - إدارة قائمة البريد وتسهيل خيار إلغاء الاشتراك بنقرة واحدة (One-click Unsubscribe).

---

## 6. 🛡️ المتطلبات غير الوظيفية والأمان (Non-Functional Requirements)

1. **الأداء والسرعة (Performance & Scalability):**
   - منع مشكلة الاستعلامات المكررة `N+1 Query Problem` باستخدام `Eager Loading` في الاستعلامات (`Article::with(...)`).
   - استخدام الـ Pagination بحدود قياسية لتخفيف الضغط على قاعدة البيانات.
   - دعم الفهرسة (Database Indexing) على الحقول المستخدمة في البحث والـ Slugs.

2. **الأمان (Security & Data Integrity):**
   - استخدام UUIDs بدلاً من المعرفات التسلسلية في الاستجابات الخارجية لمنع الثغرات والتوقع (ID Enumeration Attack).
   - التحقق الصارم من البيانات المدخلة باستخدام `Form Requests` (`StoreContactMessageRequest`, `StoreAdvertisementRequest`, الخ).
   - حماية مسارات الإدارة عبر **Laravel Sanctum Middleware**.
   - تطبيق خيار الحذف المرن `SoftDeletes` على المقالات لضمان استرجاع البيانات عند الحذف الخطأ.

3. **جودة التكوين والتطوير (Code Quality & Maintainability):**
   - التزام تام بمعايير **PSR-12** و **Clean Architecture**.
   - فصل استجابات الـ API عبر **Eloquent API Resources** لضمان عدم توفير حقول حساسة بالخطأ.
   - توفير ملفات **Postman Collections** لاختبار وتكامل واجهات الـ API بكل سهولة.

---

## 7. 🚀 خطة التطوير والتحسينات المستقبلية الموصى بها

1. **تفعيل نظام الأدوار والصلاحيات (RBAC):**
   - تفعيل موديل `Role` والميدلوير الخاص بالصلاحيات لتحديد أدوار (Admin, Editor, Author).
2. **التخزين المؤقت (Caching Layer):**
   - إدخال **Redis / Cache** للصفحة الرئيسية والأقسام الأكثر زيارة لتحسين السرعة وزيادة التحمل.
3. **تحليلات متقدمة (Analytics Dashboard):**
   - إنشاء لوحة إحصائيات لمتابعة أعلى المقالات قراءة، ومصادر الزيارات، ونسب النقر على الإعلانات.
